# dependencias
node_modules/

# build
dist/

# logs
logs/
*.log
npm-debug.log*

# editores / SO
.DS_Store
Thumbs.db
.vscode/
.idea/
.claude/

# entorno
.env
.env.*

# descargas temporales (Unsplash antes de mover a public/img)
downloads/

# material local, fuera del repo público. Con `/` inicial: solo la carpeta raíz,
# porque `public/cv/` SÍ se versiona (es el PDF generado de /resume, T8.5).
/cv/
/brand/
/referencia/

# artefactos de QA visual (Playwright MCP)
.playwright-mcp/
